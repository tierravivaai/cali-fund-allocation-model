# Pending update note

`updates/files/app.py` was not identical to the current top-level `app.py` at tidy time, so it was archived rather than treated as fully integrated.

The archived file differs in at least these areas:
- preset button 5 label/help text
- banded inversion sidebar explanatory copy
- baseline-comparison comments
- TSAC data-source note at the end of `app.py`

Review `archive/updates/files/app.py` before deleting the archived update material permanently.
